# [member]
ETCD_NAME=default
ETCD_DATA_DIR="/var/lib/etcd/default.etcd"
#ETCD_LISTEN_CLIENT_URLS="http://{{ master_hostname }}.com:{{ etcd_port_servers }}"
ETCD_LISTEN_CLIENT_URLS="http://{{ master_ip }}:{{ etcd_port_servers }}"

 [cluster]
#ETCD_INITIAL_ADVERTISE_PEER_URLS="http://localhost:2380"
# if you use different ETCD_NAME (e.g. test), set ETCD_INITIAL_CLUSTER value for this name, i.e. "test=http://..."
ETCD_ADVERTISE_CLIENT_URLS="http://{{ master_hostname }}.com:{{ etcd_port_servers }}"
